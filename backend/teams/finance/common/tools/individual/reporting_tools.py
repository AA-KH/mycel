from tools.registry.team_models import ToolImportance, AccessMode

reporting_tools = {
    "tool_id": "reporting.tools",
    "importance": ToolImportance.CORE,
    "required": True,
    "access_mode": AccessMode.FULL
}
